package com.fitness.app.domain.model

/**
 * Represents the connection state of the Smart Ring
 * Used by the Dashboard UI to toggle between Connect/Disconnect states
 */
enum class RingConnectionState {
    CONNECTED,
    DISCONNECTED
}
